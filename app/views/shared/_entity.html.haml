-# Arranges html for the abstract Entity (Mrc, Project, Client, etc).
-# Local variable 'model' used.

- e_name = model.name.downcase
- plural = e_name.pluralize

#entity-body
  #entity-nav.floatleft{:style => "width:25%"}
    = render :partial => (plural + '/' + e_name + '_navigation'), :locals => local_assigns

#entity-form.floatleft{:style => "width:75%"}
